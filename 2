print("Решение задачи 2: «Старинная задача»")

# Быки
for b in range(11):
    # Коровы
    for k in range(21):
        # Телята
        for t in range(201):
        
            total_cost = 10 * b + 5 * k + 0.5 * t
            total_animals = b + k + t
            
            if total_cost == 100 and total_animals >= 1:
                print(f"Быков: {b}, Коров: {k}, Телят: {t}")
